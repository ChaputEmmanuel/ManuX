                              -=  Manux-32  =-
                                  --------

   Le but de ce document est de stocker quelques informations sur Manux-32.
Je ne stockerai ici que les informations concernant les points instables.
Dès qu'un élément se stabilise à peu près, il est documenté dans la doc.

Feuille de route
----------------

   . Un malloc, parce qu'il y en a marre de tout déclarer en statique !
   . Mémoire virtuelle
   . Bottom halves
   . Clavier (utilisateur) à peu près utilisable
   . Modes usager/noyau
   . Bus PCI
   . Ethernet
   . Block device
   . SGF
   
En cours
--------

   A finaliser

     . Apparemment la console virtio seule marche donc à peu près, le
     reseau virtio seul semble également fonctionner (il reçoit les
     trames émises par un telnet par exemple !) mais dès qu'on
     configure les deux, c'est la panique, ... J'ai l'impression que
     le soucis est à chercher autour des handlers, mais même si on
     désactve celui du réseau, ça foire.
        Ca vient peut-être de virtio-console (ou virtio tout court)
     parce que si je décommente une ligne de printk_debug dans le
     main, même avec virtio-console seul ça plante méchamment !!!

     . Réfléchir à utiliser tout de même la consoleNoyau pour certains
     messages spécifiques. En tous cas, sur la console, il reste
     quelques points à voir :
      . toujours avoir une console active (si on a une console !)
      . plus de consoleNoyau(), on doit pouvoir faire avec
      consoleActive() ? Pas si sûr, si on peut garder les printk du
      noyau sans journal. Mais onpeut aussi dire que le journal n'est
      pas une option, ...

   Il faut alors s'attaquer à un premier système de fichiers ! Là
   je construis les inodes plutôt que le peupler ... il faut au moins
   un mknod et un lookup
     
   A ajouter : un AS qui permet de changer la console, plutôt que faire ça
   à la création.
   
   A ajouter dans l'affichage (fonction afficherEtatTaches de
   scheduler) un affichage du nombre de pages allouées, du nombre
   d'interruptions reçues, ...
   
   . Finir d'éclaircir les makefile. Voir en particulier la cible "bootage"
A faire : remplacer les .bin par des .elf  et ramener un max de choses
dans make.conf make.commons make.rules, ...

EN COURS : allocation mémoire (dont doc à restructurer).
   Changer le tabeau de proprio en un bitmap ? Permet de gagner plein
   de place et peut utiliser bsf comme ffsl

   WARNING : la première tâche conserve la pile initialement
   construite par mb-manux, ... Pas forcément un drame (elle fait 16K
   et n'est pas détruite)
   
   A FAIRE :

   Vite fait : dans mémoire, lireNombrePages et lireNombrePagesDispo
   pour affichage
   
   . utilisation de pandoc, par exemple :

pandoc --css pandoc.css boot-part.tex -f latex -t html  -s -o html/boot.html

   donne une bonne base !

   Il va falloir nettoyer un peu le code, et automatiser ça avec make
    
   (Fait, à documenter) Définition du type Temps (ressemblant à time_t) et création de
fonctions pour afficher la "date" (depuis le boot pour le moment).
Définition d'une structure TempsVal (proche de timeval) pour compter
le temps d'exécution d'une tâche.

   Pourquoi la version floppy ne fonctionne plus !? Il faudra modifier
   le code d'init pour qu'il passe au noyau les adresses de début et
   fin, comme via le linker dans la version multiboot. Peut-être là
   aussi on peut utiliser le linker ?
   
   Il faudra probablement changer include/manux en include

   Idée : utiliser les outils d'allocation mémoire pour ajouter
   (enfin) une mémoire au journal : liste datée/taguée de messages.
   
   A faire : changer un peu la getion de tacheEnCours de sorte à
   pouvoir faire la compta du temps de chaque tache dans
   basculerTache. C'est par exemple à basculerVersTache(t) de faire
   tacheEncours = t ce qui lui permet de manipuler les deux et donc
   pouvoir faire le décompte.

   A faire : clarifier entre elf et bin. changer le nom de _start en
   ManuX32 !
   
   Un petit truc à faire : un doc/Makefile
   
   Un truc à faire : des outils pour écrire en (x, y) sur la console
   gérer la position du curseur.

   Un truc moyen : bcopy et consort en asm (dans string.c) ou en #define
   dans string.h ce qui permet une cohérence entre usr et noyau !

   A faire un jour : renommer le scheduler en "tacheNoyau" et séparer
   plus clairement encore tache/fileTache/gestionTache/ordonnanceur

   Il y a manifestement un truc qui m'échappe dans la segmentation :
   si je les créé avec des indices différents, cela ne fonctionne pas!
   voir le warning dans segment.c
   
   Ca plante, et ça dépend du code que je commente, bref, ça sent
la fuite mémoire et ça pourrait être chaud à débuguer. C'est de toute
évidence le moment des'attaquer à la mémoire ... est-il normal que la
même page serve pour les données et la pile (cf tache.c) alors que
ce n'est pas le même sélecteur de segment ? Il faut faire la doc, ...
Il faut aller voir notemment dans tache.c, j'ai l'impression que c'est
n'importe quoi la gestion de la pagination ! Il faut que la structure
de la mémoire, pour le système et pour chaque tâche, soit claire. Un
affichage en fin de creerTache pourra aider, ... 
  

   ATTENTION : si _start ne finit pas à l'adresse 0x20000, le système
ne démarre pas en bootflopy car l'adresse est hard-codée dans
init-manux.nasm. Trois solutions temporaires :
   . Mettre des gros warning dans main.c pour ne pas placer de code
   avant _start (bof)
   . Tester noyau/symbols pour l'adresse de _start et arréter la
   compil si ce n'est pas la bonne valeur
   . Tester noyau/symbols et modifier MANUX_KERNEL_SEG_16 dans
   init-manux.asm
   
   SOLUTION : pour le moment, l'option -fno-toplevel-reorder a l'air
   d'y suffir. Mais il faut utiliser la dernière solution, comme pour
   la taille avec tools/taille_noyau
   

A faire
-------

   Gestion des Makefile : il faut définir le maximum dans le Makefile
de plus haut niveau, et hériter dans les autres. C'est en gros ce qui
est déjà fait, sauf usr (à revoir) qui n'a pas les mêmes include, et
outils, mais c'est moins génant, il n'utilise rien !

   . Différents "main" pour les tests. En fait, le mieux serait sûrement de
   procéder à une compilation séparée du code en mode utilisateur. Sans pour
   autant introduire déjà la notion de de format d'exécution, cela permet
   de bien établir la coupure entre les modes noyau et utilisateur. ON AIR
   . Créer tous les appels systèmes possibles afin de reporter les tests en
   mode utilisateur : fork, alloc/brk, ... ON AIR

   . Utiliser un loader classique (LILO, GRUB, ...)
   . Une terminaison propre des tâches
   . Une gestion mémoire réaliste
   . De ce fait, passer malloc en librairie
   . Un ramdisk pour pouvoir faire un FS sans disque ...
   . Mettre le code des tâches au niveau 2
   . Décider d'une arborescence correcte. Dispatcher en particulier les fichiers
   inclus dans des sous-répertoires de include, pas uniquement dans manux
   . Déterminer des règles de nommage des identifiants
   . Des makefiles propres avec des constantes factorisées
   . Etendre print[kf] ...


Les Makefile
------------

   -fno-builtin  pour éviter les prédéfinitions de printf et autres

# Les outils nécessaires

## Compilation

  * nasm : l'assembleur
  
  apt-get install nasm
  
## Lancement

### Bochs

### Qemu

   apt-get install qemu-system-i386

============================================================================

FAIT

   En cours : l'ISO plante sur un AS. Essayer de faire un AS_dumb histoire
   de tester si c'est ça ou ecrireConsole plus spécifiquement. Rêglé
   par l'utilisation de _chargerGTD  !?
   

   EN COURS :   A voir : la définition des macros est un soucis. Certaines sont
   constantes, d'autres calculées par des outils, ... Certaines sont
   utilisées en C, d'autres avec NASM, voire en ligne de commande. Il
   faut clarifier ça, surtout qu'il y a des doublons (comme
   MANUX_TEXT_SEGMENT et  MANUX_KERNEL_SEG_16 par exemple)

   je suis en train de modifier outil/taille_noyau qui devra donc être
   renommé. Il génère des macro Makefile dans taille.conf (à renommer
   aussi) en fonction de config.h Je propose donc que make.conf
   disparaisse (ou plutôt intègre ce qui était dans taille.conf) sauf
   que attention, c'est en 2 phases : l'une doit être faite en tout
   début (la génération des macrs en fonction de config.h) l'autre à
   la fin (la taille !). Donc il faut sûrement laisser les deux, ... 

   , ... il va falloir être clair sur la répartition entre
include et usr/include ! C'est (presque) une copie l'un de l'autre.
Pas très sage/safe !!! Peut-être faut-il partir du code de init.c et
voir ce qui est utile. Si ça l'est vraiment, on ajoute à une cible de
make qui copie de include/manux vers usr/include/manux.

   En cours, voir quels fichiers on peut virer de usr/include et finir
   la doc

  
   Apparemment l'erreur "Error loading uncompressed kernel without PVH
   ELF Note"  n'est pas liée aux sections puisque l'ajout de debug ne
   semble rien changer sur les sections ou les symbols (à vérifier
   plus formellement) mais induit cette erreur. Je ne sais plus comment
   je l'ai corrigée ! 
   Soucis d'exécution du kernel : grosse avancée en virant les options
   de qemu ! Et deuxième avec un bon postionnement en mémoire (le soucis
   venait d'uneincohérence de la position avec la gestin de la mémoire)
   
   . L'iso "fonctionne" mais je pense qu'il y a encore des soucis de gestion
   de la mémoire :

   mkdir ~/tmp/isodir
   mkdir ~/tmp/isodir/boot
   mkdir ~/tmp/isodir/boot/grub
   cat << EOF > ~/tmp/isodir/boot/grub/grub.cfg
   menuentry "ManuX-31" {
    multiboot /boot/manux
   }
   EOF
   cp ./noyau/noyau.elf ~/tmp/isodir/boot/manux
   grub-mkrescue -o manux.iso ~/tmp/isodir
   qemu-system-i386 -cdrom manux.iso
   
   Idée : Constuire une ISO avec plusieurs noyaux ! Par exemple avoir
   un répertoire de fichiers config.h et un make qui, pour chacun,
   fait un make clean, cp ... include/manux/config.h make noyau et qui
   à la fin construit une iso !

